import { defineConfig } from '@trigger.dev/sdk/v3'

export default defineConfig({
  // Get your project ref from https://cloud.trigger.dev → Project Settings
  project: 'proj_owvqgxdwexjdaeewzkfl',
  dirs: ['./trigger'],
  maxDuration: 120,
})
